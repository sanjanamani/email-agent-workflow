# Render Procfile
# The 'worker' process type is used for background/cron workers on Render.
# Configure the cron schedule in the Render dashboard: 0 14 * * * (9am CT = 14:00 UTC)
worker: python main.py
